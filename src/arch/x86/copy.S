#include <asm/linkage.h>

.code16

.section .text

GLOBAL(memcpy)

ENDPROC(memcpy)

GLOBAL(memset)

ENDPROC(memset)

GLOBAL(memset16)

ENDPROC(memset16)

GLOBAL(memset32)

ENDPROC(memset32)

GLOBAL(memmove)

ENDPROC(memmove)
